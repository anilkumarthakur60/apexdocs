{
    "name": "anil/apexdocs",
    "description": "Framework-agnostic OpenAPI 3.1 documentation generator. Works with Laravel, Symfony, Slim, or any PHP 8.2 project. Zero framework dependencies in the core.",
    "keywords": ["openapi", "api", "documentation", "swagger", "php", "laravel", "symfony", "postman", "insomnia", "bruno", "webhook"],
    "license": "MIT",
    "type": "library",
    "homepage": "https://github.com/anilkumarthakur60/apexdocs",
    "authors": [
        {
            "name":     "Anil Kumar Thakur",
            "email":    "anilkumarthakur60@gmail.com",
            "homepage": "https://github.com/anilkumarthakur60",
            "role":     "Maintainer"
        }
    ],

    "require": {
        "php": "^8.2",
        "phpstan/phpdoc-parser": "^1.0 || ^2.0",
        "symfony/yaml": "^6.4 || ^7.0 || ^8.0",
        "psr/simple-cache": "^3.0",
        "psr/http-message": "^2.0",
        "psr/http-server-handler": "^1.0"
    },

    "require-dev": {
        "pestphp/pest": "^3.0",
        "pestphp/pest-plugin-laravel": "^3.0 || ^4.0",
        "laravel/framework": "^11.0 || ^12.0",
        "orchestra/testbench": "^9.0 || ^10.0",
        "nyholm/psr7": "^1.8",
        "symfony/config": "^6.4 || ^7.0",
        "symfony/dependency-injection": "^6.4 || ^7.0",
        "symfony/http-kernel": "^6.4 || ^7.0",
        "symfony/routing": "^6.4 || ^7.0"
    },

    "suggest": {
        "laravel/framework": "Required for the Laravel bridge (ApexDocs\\Bridge\\Laravel)",
        "symfony/framework-bundle": "Required for the Symfony bridge (ApexDocs\\Bridge\\Symfony)",
        "symfony/routing": "Required for the Symfony route collection",
        "psr/simple-cache-implementation": "Needed only to cache the built spec via ApexDocs\\Cache\\SpecCache",
        "psr/http-factory-implementation": "Needed only for the PSR-15 handler (ApexDocs\\Http\\Handler)"
    },

    "bin": ["bin/apexdocs-mcp"],

    "autoload": {
        "psr-4": {
            "ApexDocs\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "ApexDocs\\Tests\\": "tests/"
        }
    },

    "extra": {
        "branch-alias": {
            "dev-main": "0.x-dev"
        },
        "laravel": {
            "providers": ["ApexDocs\\Bridge\\Laravel\\ServiceProvider"],
            "aliases":   {"ApexDocs": "ApexDocs\\Bridge\\Laravel\\Facade"}
        }
    },

    "config": {
        "sort-packages": true,
        "allow-plugins": {
            "pestphp/pest-plugin": true
        }
    },
    "minimum-stability": "stable",
    "prefer-stable": true,
    "scripts": {
        "test":          "vendor/bin/pest",
        "test-coverage": "vendor/bin/pest --coverage",
        "test-filter":   "vendor/bin/pest --filter",
        "security":      "@composer audit --no-dev --locked --abandoned=report",
        "lint":          "find src tests -name '*.php' -print0 | xargs -0 -n1 php -l",
        "ci":            ["@lint", "@test", "@security"]
    },
    "scripts-descriptions": {
        "test":          "Run the full Pest suite.",
        "test-coverage": "Run the suite with HTML+text coverage output.",
        "test-filter":   "Run a subset of tests matching a substring.",
        "security":      "Check runtime deps for known CVEs (requires composer.lock).",
        "lint":          "PHP syntax-check every file in src/ and tests/.",
        "ci":            "Lint + Test + Security audit. What CI runs."
    },
    "support": {
        "issues": "https://github.com/anilkumarthakur60/apexdocs/issues",
        "source": "https://github.com/anilkumarthakur60/apexdocs",
        "docs":   "https://github.com/anilkumarthakur60/apexdocs#readme"
    },
    "funding": [
        {
            "type": "github",
            "url":  "https://github.com/sponsors/anilkumarthakur60"
        }
    ]
}
